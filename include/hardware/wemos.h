#pragma once
#include "hardwareProfile.h"

const HardwareProfile hardwareProfile = {
    .modelo = "WEMOS",
    .board = "d1_mini",
    .ledPin = 13,
    .ledInvertido = false,
    .relePin = 2,
    .botaoPin = 255,
};
