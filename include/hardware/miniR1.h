#pragma once
#include "hardwareProfile.h"

const HardwareProfile hardwareProfile = {
    .modelo = "MINIR1",
    .board = "esp01_1m",
    .ledPin = 13,
    .ledInvertido = true,
    .relePin = 12,
    .botaoPin = 255,
};
